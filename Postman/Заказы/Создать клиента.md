https://{{URL}}/api/orders/v1/Client

{
    "nic": "{{NAME_CLIENT}}",
    "company": {
        "id": "{{COMPANY_ID}}",
        "nameEn": "{{COMPANY_NAME_EN}}",
        "nameRu": "{{COMPANY_NAME_RU}}",
        "__typename": "{{COMPANY_TYPENAME}}"
    },
    "tariff": {
        "t1Price": 35,
        "exPrice": null,
        "deliveryPrice": 0,
        "pickUpPrice": 3010,
        "insurance": 0,
        "tariffPerKg": 11,
        "tapePrice": 3,
        "citesPrice": 140,
        "controlMarkFursPrice": 150,
        "controlMarkSheepskinsPrice": 100
    },
    "printMark": false,
    "partnerWarehouses": [],
    "partnerCompanies": [],
    "tags": [],
    "clientCodes": null,
    "clientCompanies": null,
    "clientTransits": null,
    "contactInfos": null
}

pre-request
let randomDigits = Array.from({length: 10}, () => Math.floor(Math.random() * 10)).join("");
let today = new Date(); // Fri Oct 17 2025 14:35:08 GMT+0000 (Coordinated Universal Time)
let iso = today.toISOString(); // 2025-10-17T14:35:08.123Z
let isoDate = today.toISOString().split('T')[0] // 2025-10-17

let name = iso + "-CLIENT-AM";

pm.environment.set("NAME_CLIENT", name);

post-response
let response = pm.response.json();

pm.environment.set("CLIENT_ID", response.id);
pm.environment.set("CLIENT_NIC", response.nic);
pm.environment.set("CLIENT_CREATED_DATE", response.createdDate);

pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});