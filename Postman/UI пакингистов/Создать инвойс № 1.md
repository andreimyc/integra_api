Создать инвойс № 1

https://{{URL}}/api/invoicer/invoices/

{
    "number": "{{NAME_INVOICE}}",
    "date": "{{CURRENT_DATE}}",
    "type": {
        "id": "INVOICE",
        "nameRu": "Инвойс"
    },
    "endCustomer": {
        "name": "",
        "country": null
    },
    "sender": {
        "name": "",
        "country": {
            "alpha2": "{{COMPANY_COUNTRY_ALPHA2}}",
            "id": "{{COMPANY_COUNTRY_ID}}",
            "name": "{{COMPANY_COUNTRY_NAME}}",
            "__typename": "{{COMPANY_COUNTRY_TYPENAME}}",
            "nameRu": "{{COMPANY_COUNTRY_NAME}}",
            "nameEn": "{{COMPANY_COUNTRY_NAME}}"
        },
        "company": {
            "eori": {{COMPANY_EORI}},
            "id": "{{COMPANY_ID}}",
            "name": "{{COMPANY_NAME}}",
            "vat": "{{COMPANY_VAT}}"
        }
    },
    "recipient": {
        "name": "",
        "country": null
    },
    "incoterms": null,
    "currency": {
        "isoCode": "EUR"
    },
    "sum": 100,
    "itemCount": 2,
    "packingCount": 3,
    "grossWeight": 4,
    "netWeight": 8,
    "documents": [
        {
            "id": "{{FILE_ID}}",
            "fileName": "{{FILE_NAME}}",
            "documentType": "{{FILE_CONTENT_TYPE}}",
            "fileStoragePath": "{{FILE_STORAGE_PATH}}"
        }
    ],
    "orders": [],
    "wayBills": [],
    "citeses": [],
    "exportDeclarations": [],
    "inEdit": true,
    "volume": 6,
    "client": {
        "nic": "{{CLIENT_NIC}}",
        "id": "{{CLIENT_ID}}",
        "__typename": "{{CLIENT_TYPENAME}}"
  }
}

Pre-request
let randomDigits = Array.from({length: 10}, () => Math.floor(Math.random() * 10)).join("");
let today = new Date(); // Fri Oct 17 2025 14:35:08 GMT+0000 (Coordinated Universal Time)
let iso = today.toISOString(); // 2025-10-17T14:35:08.123Z
let isoDate = today.toISOString().split('T')[0] // 2025-10-17

let name = iso + "-AM";
let date = isoDate + "T00:00:00.000Z";

pm.environment.set("NAME_INVOICE", name);
pm.environment.set("CURRENT_DATE", date);

Post-response
let response = pm.response.json();

pm.environment.set("INVOICE_ID_1", response.id);

pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});