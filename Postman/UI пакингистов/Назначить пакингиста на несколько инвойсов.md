https://{{URL}}/api/invoicer/invoices/batch/packing/assign-to-user

{
    "invoicesIds": [
        "{{INVOICE_ID_1}}",
        "{{INVOICE_ID_2}}"
    ],
    "userId": "{{USER_ID_IS_ROLE}}"
}

pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});